# filedeaze-nest-backend
# Watch mode (auto-reloads on changes)
npm run start:dev

# After schema changes
npm run prisma:migrate   # creates migration
npm run prisma:generate  # regenerates client

# Re-seed
npm run prisma:seed
